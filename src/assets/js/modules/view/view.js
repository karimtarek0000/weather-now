import $ from 'jquery';

// RENDER CUR LOCATION
export const renderCurLocation = (curState, curCountry) => {

    // CONST
    const curCountryNow = $('#curCountry');
    const curStateNow = $('#curState');

    // ADD CUR LOCATION FROM DATA API TO DOM CUR STATE AND CUR COUNTRY
    // curStateNow.text(curState);
    // curCountryNow.text(curCountry);
    console.log(curState);
    console.log(curCountry);
}

